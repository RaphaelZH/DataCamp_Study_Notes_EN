# run: conda env create --file nlp_practices_env.yml
name: nlp_practices
channels:
  - conda-forge
  - defaults
dependencies:
  - autopep8=1.5.7 # pin version for conda
  - click=7.1.2 # pin version for conda
  - ipyparallel=6.3.0 # pin version for conda
  - jupyter=1.0.0 # pin version for conda
  - jupyter_contrib_nbextensions=0.5.1 # pin version for conda
  - matplotlib=3.4.2 # pin version for conda
  - nbconvert=5.6.1 # pin version for conda
  - nltk=3.6.2 # pin version for conda
  - notebook=6.4.0 # pin version for conda
  - numpy=1.19.5 # pin version for conda
  - pandas=1.2.1 # pin version for conda
  - pip=21.1.2 # pin version for conda
  - python=3.7.9 # pin version for conda
  - scikit-learn=0.24.1 # pin version for conda
  - spacy=3.0.6 # pin version for conda
  - tensorflow=2.4.1 # pin version for conda
  - yapf=0.31.0 # pin version for conda
  - pip:
    # works for regular pip packages
    - cython==0.29.23 # pin version for pip
    - spacy-pkuseg==0.0.28 # pin version for pip
prefix: /Users/haozhang/opt/anaconda3/envs/nlp_practices